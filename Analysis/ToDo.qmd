---
title: "ToDo"
format: html
---

- Clean the save_plot function to only receive the name of the plot and construct the full name inside the function
- Finish function to show the number of cells by cluster
- Create a save patient data function to be in the same chunk as the save_plots function
- When saving patient objects, save just one object with all the data (no sub folders per patient)
- Create a plotting.R file with the code used for plotting
- Investigate the function Seurat::SplitObject to split the object by patient
- Print the VariableFeaturePlot for each patient's RNA assay
- Download InSituType locally, modify the description file to add the dependency on the "lsa" package and install it again
